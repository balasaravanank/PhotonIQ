# ============================================================
#  SOLAR OPTIMIZER - Environment Variables
#  Copy this file to .env and fill in your values
# ============================================================

# Server
PORT=3001

# USB SERIAL (Arduino connected via USB cable — no WiFi needed!)
#
# HOW TO FIND YOUR PORT:
#   Windows → Device Manager → Ports → "Arduino Uno (COM3)"
#   Linux   → ls /dev/tty*  → usually /dev/ttyUSB0 or /dev/ttyACM0
#   Mac     → ls /dev/cu.*  → usually /dev/cu.usbmodem14101
#
# IMPORTANT: Close Arduino Serial Monitor before running node server.js!
SERIAL_PORT=COM3
BAUD_RATE=9600

# Firebase (get from Firebase Console → Project Settings → Service Accounts)
FIREBASE_DB_URL=https://your-project-id-default-rtdb.firebaseio.com

# WeatherAPI.com (https://www.weatherapi.com → Sign up → API Key)
WEATHER_API_KEY=your_weatherapi_key_here
WEATHER_CITY=Chennai
